import { Character } from './tutorial.model';
import { Services } from './querys.service.marvel.model';


describe('Character', () => {
  it('should create an instance', () => {
    expect(new Character()).toBeTruthy();
  });
});

describe('Services', () => {
  it('should create an instance', () => {
    expect(new Character()).toBeTruthy();
  });
});
